# Photography — drop client-supplied and licensed placeholder imagery here

| File | Purpose |
|---|---|
| `hero-poster.avif` | Hero LCP image — loads instantly, must be the `priority` image |

Property photography, Scott's portrait, and neighborhood imagery get their own subfolders as
those phases start (`/property`, `/neighborhoods`, `/about`). Until real assets exist, use
license-clear Unsplash/Pexels Key West imagery per build brief §6, and mark the file or its
usage `TODO-CLIENT-ASSET` so it's easy to find and swap later.
