# Website assistant and offer intake

Status: written suggestions implemented; the website now sends the same broker-supplied guidance directly into each new offer voice/text session. Two live text API test sessions verified the recommendations. Remote ElevenLabs dashboard publication remains unavailable, but the website no longer depends on that update to provide Scott's guidance. The published remote tool still has 11 parameters, so three newer separate fields may need completion in the written questions. Automatic n8n transfer, avatar integration, and production activation remain outstanding.

## Scott's corrected recommendations, September 14

Scott supplied the corrected guidance directly in this conversation, superseding the earlier missing-guide search. Ask property address, offer price and escrow first. The written flow includes these suggestions with explicit accept/edit controls; none is preselected:

- Escrow: suggest 20% for a strong offer; below 10% can make the offer look weak when presented.
- Inspection: recommend seven days, no more than 15; 15 days can significantly weaken the offer. This is Scott's advice, not a legal maximum.
- Title insurance specifically: typically seller-paid but negotiable; suggest proposing seller pays. Do not apply this to every closing cost.
- Closing company: suggest Greg Oropeza; whoever pays title insurance gets to choose. Record Greg as a proposal subject to the payer's choice.
- Cash: suggest closing in 20 days if there are no title issues.
- Financed: suggest 30 days for the financing contingency, plus 15 additional days to close. Keep these terms separate, and do not invent a calendar date.
- Special clause: seller to close all open permits, including tree permits, before closing at seller's expense. Adding this suggestion preserves other buyer requests.

The intake now has 14 fields, or 13 for cash purchases, which skip the financing contingency. Changing cash/financing clears stale closing and contingency answers unless new timing is explicitly supplied in the same update. The review queue accepts older records without inventing answers to the newly separated questions.

`offer-agent-prompt.md` and `offer-agent-client-tool.json` contain the exact proposed remote-agent update. It must still be applied, published and verified. The new written questions collect any fields the earlier agent cannot populate.

Header correction deployed separately at `https://scott-homes-l4kmkpqky-royalpalmsrealty1.vercel.app`, deployment `dpl_21SBUXJhFmJfAecyF8hUHmR4PCgz`, source commit `6cd2f66`. Original desktop navigation and home-value button classes are restored; Make an Offer is in its own row; phone navigation has a visible Menu label and home-value link. Vercel build and TypeScript passed. An authenticated HTTP fetch returned 200 and confirmed all ten navigation labels and both responsive home-value links. Browser visual verification was unavailable because tab calls timed out. Header ESLint also reports the pre-existing `react-hooks/set-state-in-effect` issue on the unchanged mobile-dropdown reset effect.

## Website behavior

- The header and existing listing-chat panel open a site-wide Make an Offer side panel.
- General voice/text conversations use the existing public receptionist `agent_4201m294fbxkf2qs82samt0bfwv7` through the ElevenLabs React SDK.
- ElevenLabs conversations require the visitor's consent and start action. Closing the panel ends the session. Typed offer answers remain in memory until refresh or Start over; personal offer details are not written to browser storage.
- The written offer interview collects property address, offer price, escrow, payment method, inspection, title-insurance payer, proposed closing company, financing contingency when applicable, closing timing, conditions, buyer names, email, phone and representation.
- Property references are buyer supplied and explicitly unverified. Cross-origin IDX iframe content is never accessed or treated as a verified property record.
- Buyers must review and confirm their terms before submission. Voice tools can only suggest draft fields; no tool can confirm or submit.
- Requests are inserted into the existing RLS-protected `leads` table with source `website-offer-intake`. A duplicate request identifier cannot overwrite another request. There is no public request read/update endpoint.
- The authenticated `/admin/offer-requests` page displays requests. It fails closed if Supabase or SESSION_SECRET is missing. The submission endpoint reports success only after a durable database write.

## Published offer agent

ElevenLabs agent `agent_2101m2gd2zqde1bra29706q5n1fz`, Royal Palms Realty - Website Offer Assistant, is published with the Eric voice and the single client tool `update_offer_draft`. Its Main branch is `agtbrch_7801m2gd2zqseaja4wyzd7egbkp6`. The existing receptionist was not modified. The offer agent has no rental, owner-command, appointment, transfer, or external messaging tools.

`NEXT_PUBLIC_OFFER_AGENT_ID` is configured in the existing Vercel project for Preview and Production. The value is the public offer-agent identifier above. This does not deploy the website changes to production by itself.

Use an offer-specific public agent or configured receptionist transfer. Do not publish the private property-management agent or enable phone-only owner commands for a website visitor.

Without `NEXT_PUBLIC_OFFER_AGENT_ID`, the written intake remains available and the offer voice option stays hidden. General receptionist voice/text remains separate. The offer agent was tested through a real website text session: all 11 buyer-provided sample answers appeared correctly in the editable review form. Microphone quality must still be tried on the visitor's own device.

The client tool `update_offer_draft` has `expects_response: true` and a 15-second response timeout. Its parameters are optional strings named exactly: propertyReference, buyerName, buyerEmail, buyerPhone, offerPrice, financing, deposit, inspection, closingDate, conditions, representation. The browser handler rejects unknown keys. ElevenLabs' dashboard JSON editor uses an array of `{id, type, description, required, value_type, dynamic_variable, constant_value}` parameter entries, rather than the API's object JSON schema. Do not paste the API schema into that dashboard editor.

The corrected proposed prompt is in `offer-agent-prompt.md`; it has not replaced the previously published configuration. The new tool parameters additionally include titleInsurance, closingCompany and financingContingency. Update the remote tool before relying on it to populate those separate fields.

## Existing n8n workflow

`KOVJllCGeHz613gK` (RPR Florida AS-IS Offer — Broker Review v2) remains unchanged. Its confirmed production form is `https://scottforman.app.n8n.cloud/form/5f03d1a2-cd72-4926-b5ee-65c96b1e2286`. Each request in the private review screen links to this form for manual broker completion; no buyer data is placed in URLs. Automatic transfer has not been implemented. Keep its exact-address Spark property match, legal-description resolution, broker review, and DocuSign block intact. Never mark a website request as a prepared offer merely because it was saved.

The workflow's actual inputs were inspected: Buyer 1 legal name; Buyer 1 email; Additional buyer legal name(s); Property street address (include unit if applicable); City; County; Florida ZIP code; Offer price; Initial escrow percent; Additional escrow percent; Escrow agent / title company; Financing; Loan amount or LTV; Inspection period (days); Closing date; Offer expiration date and time; Seller contribution / credit; Personal property included; Additional terms / agent notes; Property type; HOA or community association; Built before 1978; Buyer-broker compensation document needed. The website request intentionally preserves uncertain proposals for broker review. An automatic adapter must collect and validate these remaining structured fields rather than guessing percentages, dates, property attributes, or compensation choices.

Before production activation, verify secure notification/delivery to the existing review workflow and a real broker receipt using a clearly labeled test authorized for delivery. No emails or texts were sent during local validation.

## Deployment and verification

The live Vercel project is `prj_nveCkg6sUJjoSSZw1Qv0vf0Jx8VX`, team `team_Q7u5kzOoGA1mR5xbVimENBYL`. Production source matches `royalpalmsrealty/scott-homes-v1` commit `345a7d2`; Vercel's Git link refers to a different repository. Deploy this source explicitly. Do not push to the other repository.

Required server environment: existing SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, SESSION_SECRET. Optional offer voice activation: NEXT_PUBLIC_OFFER_AGENT_ID. Keep service-role credentials server-side.

Passed local validation: `node --test tests/offer-intake.test.mjs` (six tests), `node tests/offer-intake-http.mjs` (foreign-origin rejection, confirmation required, oversized-body rejection, unavailable-storage failure, and private-review login redirect), targeted ESLint, TypeScript, and `npm run build`. The HTTP test launches its own server with storage credentials explicitly disabled; no leads are written. Browser testing must cover desktop/mobile layout, IDX coexistence, focus and Escape handling, microphone denial and cleanup, text-only sessions, transcript accuracy, edit/reset behavior, durable submission and idempotent retry, and broker-only access. Localhost was blocked by the cloud browser, so a Vercel preview is required for that verification.

Vercel CLI and ElevenLabs sign-in are complete. CLI is linked to `royalpalmsrealty1/scott-homes` with the project/team IDs above. The first preview, `dpl_67bUsTDujj7oN4iE5q7JZBCKWc4i`, built successfully at `https://scott-homes-otlom3o8n-royalpalmsrealty1.vercel.app`. Use authenticated Vercel CLI deployment; GitHub push remains unauthenticated. Do not commit `.env.local` or CLI credentials.

Earlier interview preview: `https://scott-homes-iiynz201w-royalpalmsrealty1.vercel.app`, deployment `dpl_8qiM9U856WsrnewUxNveJwciYLD9`, source commit `f856ec0`. The Vercel build passed and is READY. This includes the two interview refinements below. The final cloud-browser connection timed out, so those refinements have static TypeScript/lint/build validation but no additional browser pass. Production was not changed.

Browser validation on the preview: consent gates the AI start; a real text conversation filled all 11 sample answers; review displayed exact dollar amounts and proposed terms; the send button required explicit confirmation; a clearly labeled fictional test saved successfully with receipt `cd75d7ba-3a24-4ab3-ba99-aead37895a1f`. No email, SMS, contract generation, or seller submission was triggered. Two follow-up interface fixes add typed user messages to the transcript and jump from the completed interview to review (or the first incomplete field).

The saved test was independently confirmed in Supabase: source `website-offer-intake`, status `awaiting_broker_review`, buyerConfirmed true, propertyVerified false. Escape restored focus to the header button, and reopening retained the receipt. Mobile-device and live-microphone testing remain outstanding.

## Corrected written guidance validation

September 14: all 11 schema/behavior tests passed, covering explicit acceptance, cash versus financed timing, cleared stale timing, preservation of other special requests, and old saved-request compatibility. The five HTTP guards passed with storage disabled. TypeScript, targeted ESLint and the production build passed. The header source is unchanged from the corrected header deployment. Browser calls still time out, so there is no new visual or voice-session verification. The matching remote-agent changes remain prepared and unpublished.

Corrected written-guidance preview: `https://scott-homes-iv75s0j3k-royalpalmsrealty1.vercel.app`, deployment `dpl_6aqxdN2MdvTAXjMgAmwTtdfNGegW`, source commit `2a662f4`. Vercel reports READY and a successful production build. Authenticated HTTP fetch returned 200; all ten header navigation labels, the home-value link and Make an Offer are present. Production is unchanged. No additional requests, emails or text messages were sent.

## Voice recommendations and visible navigation correction

Scott reported that the agent deferred the deposit recommendation to him and that only two header actions were visible. The earlier HTTP assertion proved the navigation existed in HTML but missed its `hidden lg:block` rule. The header now displays one wrapping primary navigation at every width, with all ten original destinations, the home-value action and Make an Offer. Buyers/Sellers retain dropdowns; touch input opens them by tap. Call Scott and Book a Call remain available on the phone header. No screenshot or device-width visual pass was possible because the browser connection timed out.

The dedicated website agent receives `offerAgentContext` through the installed ElevenLabs SDK's `onConversationCreated` lifecycle hook and `sendContextualUpdate`, before visitor input. It uses the same `offerGuidance` values as the written form and explicitly instructs proactive recommendations, including the below-10% escrow warning. Both voice and text start paths use this hook. This does not change the original phone receptionist or claim that the dashboard prompt was published.

Two live text sessions using the public offer-agent API and the exact lifecycle/context code passed `node tests/offer-agent-live.mjs --live`. The agent proactively proposed 20% and warned that below 10% weakens the offer; answered seven days/no more than 15 with the weakening warning; recommended seller-paid title insurance and Greg Oropeza subject to the payer's choice; suggested 20-day cash closing if no title issues; supplied the full seller-paid open-permit/tree-permit clause; and proposed 30 days plus an additional 15 for financing. All scenarios used fictional test data and local tool handlers; no website lead, email, SMS or contract was created. Microphone capture and playback were not tested.

Local production build, TypeScript, targeted ESLint, all 11 schema tests and the HTTP guards passed. The HTTP test additionally asserts the ten ordered labels and rejects navigation hiding rules on the rendered navigation or its ancestors. Remote publication of the prepared 14-parameter client tool remains pending; the website still requires explicit completion of any fields absent from that older tool.

Latest preview: `https://scott-homes-fcmwco5xv-royalpalmsrealty1.vercel.app`, deployment `dpl_CvxwVfNKvARSgXvkmDk8GXU6PJyN`, source commit `3020bc2`. READY with a successful Vercel build. Authenticated HTTP returned 200 and confirmed all ten ordered navigation labels with no hiding classes on them or their ancestors, plus both header actions. Production remains unchanged. Start a fresh offer session on this preview to receive the website-supplied guidance.

## September 14 — concise intake, phone recovery and MLS handoff

The opening written question now asks whether the buyer is working with another real estate agent. No proceeds immediately to the property question and enables the AI conversation, without a representation speech or acknowledgement. Yes/unsure pauses intake for office coordination. The server rejects unresolved-agent submissions; AI patches cannot rewrite No as self-representation. New agent disclosure pauses the flow. Legacy saved requests remain readable.

All approved recommendations now use “we suggest” and short turns. `getOfferSessionDetails` sends the current entered draft and next missing question as supported ElevenLabs context, so a reconnect can continue from retained answers. The exact live text test passed: No → property; transaction-broker answer only when asked without added legal explanation; brief finish; all cash/deposit/inspection/title/company/permit and financed-contingency suggestions. The remote dashboard remains unchanged. A preliminary probe incorrectly treated connection creation as override permission; subsequent disconnect events conclusively showed close 1008 for both prompt and first-message overrides. Those overrides were removed. Do not reintroduce them without a properly verified remote configuration change.

Screen Wake Lock now covers the whole open offer intake, including written review and text conversations. It releases on close/background and retries on return or a tap even if the first request was denied. Late acquisition after close releases immediately. The receptionist uses the same controller only while connected/connecting; SDK duplicate wake handling is disabled. Interrupted conversations preserve answers in memory and offer reconnect/resume. Refresh or browser-process termination still clears the draft; no buyer information is persisted in browser storage. Actual iPhone screen locking and microphone/audio remain untested because the cloud browser cannot return editor/page state.

MLS handoff code is implemented: exact-source candidates, signed address-bound selections, server-side record refresh at submission, full private property/agent/office packet in the existing leads payload, and authenticated broker download. See `offer-mls-handoff.md` for the concrete n8n bridge contract and outstanding activation. **It is not a live MLS connection yet.** The saved `Flex MLS` credential exists in n8n; Vercel has no MLS lookup URL/token. No n8n workflow, credential, email or automated delivery was changed. An unavailable lookup is clearly unverified and never invents property facts or a FlexMLS link. Header source remains unchanged from the fully visible navigation correction.

Validation: 22 local unit tests and targeted ESLint passed; live ElevenLabs text cases passed with fictional data and local-only draft handlers. Production build, HTTP guards and deployment results are recorded below after completion. No new lead or outbound communication was created during these tests.

Updated preview: `https://scott-homes-p6enbn27s-royalpalmsrealty1.vercel.app`, deployment `dpl_5fjEckrWVKv1vBjt17mJbaJ3vqWB`, source commit `96fc021`. Vercel reports READY; remote build completed in 22 seconds. Local build/TypeScript and HTTP guards passed, including unconfigured MLS behavior and private packet authentication. Authenticated deployed HTTP returned 200; all ten ordered navigation destinations, home-value and Make an Offer are present without navigation hiding classes; the opening other-agent question is rendered. No new browser visual/device test. Production remains unchanged. Live n8n MLS bridge activation and the remote 14-field ElevenLabs tool update remain pending.

## September 14 — repeated voice cutoffs

Scott reports that the offer agent begins a response, stops, and starts again as though interrupted. Background sound or speaker echo is a hypothesis, not a reproduced device diagnosis. The installed ElevenLabs WebRTC input already requests echoCancellation, noiseSuppression and autoGainControl. No remote interruption/VAD settings were changed; there is no usable authenticated editor connection in this session.

Added website offer-voice protection using supported SDK microphone mute controls: pause input during agent speech, keep it paused through brief audio gaps, then reopen after 200 ms of listening mode. A visible Speak now control permits a deliberate interruption. Manual mute persists across turns; ending the session cancels pending microphone changes. This changes offer-voice turn behavior: the visitor normally waits until Listening — your turn, or taps Speak now to speak over the agent. Text and the separate receptionist retain their existing behavior.

Three control tests passed: short playback-gap protection and automatic reopening; deliberate interruption followed by re-arming; persistent manual mute and cleanup. Targeted ESLint and the local production build/TypeScript passed. No actual speaker/microphone or iPhone reproduction was possible, so this is a tested control-path mitigation awaiting a phone trial, not a proven device-level resolution. The server's interruption sensitivity remains unchanged.

Voice-interruption preview: `https://scott-homes-qjbmrthdf-royalpalmsrealty1.vercel.app`, deployment `dpl_9hWsTreKdqnC6ZQDQQqF2Ga7yMiv`, source `b76580e`. Vercel READY; remote build completed in 15 seconds; authenticated HTTP returned 200. The installed LiveKit default stopMicTrackOnMute is false, so ordinary mute/unmute preserves the microphone track. Actual phone cutoffs still require a fresh voice-session trial. Production is unchanged.


## September 14 — intermittent hearing and address corrections

Scott's phone trial of source b76580e reported intermittent hearing, repeated waiting prompts and refused address corrections. That is evidence against the preceding automatic microphone-gating mitigation. Removed the gate, its Speak now control and its control-path tests; microphone input now remains enabled throughout a voice session unless the visitor explicitly mutes it. SDK echo cancellation and noise suppression remain enabled. This rollback is not a verified resolution of speaker echo or server interruption sensitivity. A phone trial is still needed.

The client tool now uses the SDK's latest-handler hook, so an active conversation receives current form callbacks instead of a session-start closure. Draft changes and current property lookup status are sent as supported contextual updates without restarting speech or generating an additional user turn. Buyers can use Change address and Save address at any point during an active intake, including while speaking. Saving invalidates the prior listing selection and confirmation, keeps the other terms, and starts a new lookup. An unsaved address edit blocks final confirmation/submission. The agent guidance expressly permits corrections, replaces prior addresses and discourages repeated waiting prompts.

An absent MLS connection now produces the distinct API state not_connected. The form says that MLS lookup is not connected and that no listing details were retrieved. A real empty search has state not_found; a failed lookup is unavailable. None means the address is invalid or needs broker permission. The n8n read-only bridge is still not activated and Vercel still has no lookup URL/token. An additional browser tabs request timed out; no editor or authentication screen was reached. Remote voice/VAD settings and the 14-field tool remain unpublished.

Validation: 24 unit tests passed, including address replacement with other terms retained and distinct disconnected/empty-search statuses. A live fictional ElevenLabs text session accepted a request to change the address, updated propertyReference to the supplied replacement while retaining the price, and subsequently read the latest form-edited address and the original $1,000,000 price. Tool calls were local mocks; no lead or external communication was created. This verifies agent text/tool behavior, not phone audio or browser rendering. Deployment and final HTTP results follow below.

Updated preview: `https://scott-homes-9upqlqkqe-royalpalmsrealty1.vercel.app`, deployment `dpl_GxJFSJ3Gb415GYKcpNRCGsaMoYp9`, source `ea645fb`. Vercel READY, remote build 17 seconds. Local build/TypeScript, targeted ESLint and HTTP guards passed. Authenticated deployed homepage and read-only property-lookup endpoint both returned HTTP 200; the latter returned exactly `{state:"not_connected",candidates:[]}`. All ten navigation destinations, home-value and Make an Offer remain rendered without hiding rules. No production promotion or browser/device/audio verification. Start a fresh conversation on this preview for the new session guidance.


## September 14 — one-button voice entrance and spoken existing-agent question

Scott requested a shorter entrance and then explicitly moved the existing-agent question from the form to the voice conversation. Make an Offer now opens the voice introduction directly, with a disclosure and Agree & start speaking button. No other-agent form, address field, separate reveal button or extra consent checkbox precedes the offer voice session. Written entry remains an explicit secondary choice. The written wizard and its progress indicator stay out of the conversation view. Transcript and Type a reply are expandable; Review my answers ends the session and opens the next unanswered field or completed review. Existing answers, corrections, wake handling, MLS states and final confirmation remain in place.

The client tool may now record an explicit No/Yes/unsure before offer terms. Missing or unrecognized representation does not permit storing property/offer fields; Yes/unsure pauses. No is never a default and still must be present for submission. The voice session remains mounted through this answer so the agent can speak its next question. Optional written entry retains the accessible No/Yes/unsure choice.

Live fictional text tests passed: a visitor-provided address with no agent answer leads to the spoken agent question without saving the address; No then saves the previously stated address and asks price without repeating it; Yes is recorded and pauses offer terms. These used local tool mocks and created no request or outbound communication. Sixteen intake unit tests passed.

Known blocker: the remote ElevenLabs fixed first message still says “I'm Royal Palms Realty's AI offer assistant. Which property would you like to make an offer on?” Session context cannot replace that fixed greeting; overrides remain disallowed. Thus the existing-agent question is spoken, but the exact desired first-question order is not live yet. Prepared remote First message: “Are you working with another real estate agent on this purchase?” Browser setup succeeded but tab discovery timed out and reset the connection; troubleshooting guidance offered no applicable recovery. No remote agent settings were changed. Browser visual interaction and phone audio remain unverified.

Local production build/TypeScript, targeted ESLint and HTTP guards passed using the same public offer-agent ID as Preview. The first local HTTP run used an unconfigured build, correctly showing the written fallback; rebuilding with the Preview public agent ID proved the default voice entry has no representation form or address field. The full navigation remains rendered without hiding rules.

Voice-entry preview: `https://scott-homes-1cmhcaech-royalpalmsrealty1.vercel.app`, deployment `dpl_4MpGa5u3SWeAEuYBcSRT4GGCbDLu`, source `b03b166`. Vercel READY, remote build 16 seconds. Authenticated HTTP returned 200 and verified no initial representation form/address input, with all ten navigation destinations and home-value/Make an Offer retained without hiding rules. This does not verify interactive browser rendering or phone audio. Production unchanged. The fixed ElevenLabs first-message edit remains pending.


## September 14 — direct editable summary

Scott requested that finishing the conversation go straight to a summary with editable answers, without repeating the question wizard. Removed question-step state and the wizard from OfferIntake. Review my answers now ends the voice session and shows OfferReview immediately, even if fields are missing. The same summary is used when a visitor chooses to type. All saved answers appear in editable controls together; missing/invalid values are marked in place. No Back/Continue sequence or second Edit step remains. Approved suggestions are optional, collapsed and shown only for unanswered fields, so already chosen terms are not presented for another choice.

Changing an answer clears the final confirmation. Cash/financed timing validation, the existing-agent gate, property-selection validation and explicit buyer submission remain in effect. Address edits invalidate the prior MLS selection and perform lookup on blur; the voice-only quick address editor transfers its pending text into the summary rather than losing it. The summary focuses its heading when opened. Neither the remote fixed opening nor the pending MLS integration was changed.

Validation: the actual OfferReview TSX component was rendered with complete, incomplete, financed and other-agent fixtures. Saved values stayed exact and editable, missing fields remained on the same page, completed answers did not receive another suggestion, and required timing/agency/property/confirmation states blocked sending. All 21 relevant intake/property unit tests passed. These are render/control tests, not an interactive browser or phone trial.

Editable-summary preview: `https://scott-homes-3u6ybkj63-royalpalmsrealty1.vercel.app`, deployment `dpl_7sHkCzSH3SwZG54Fo5ZhKYbowQx6`, source `ef78684`. Vercel READY; remote build 15 seconds. Local production build/TypeScript, targeted ESLint and HTTP guards passed. Authenticated deployed homepage returned 200 and retained the voice entrance, all ten primary navigation destinations, home-value and Make an Offer. Production unchanged. Summary behavior is supported by actual component render tests and source-flow review; no new browser or phone trial was performed.
## September 14 — standby recovery and access to Scott

Scott reported that the phone still enters standby mid-conversation. The previous controller only recovered a released or denied screen wake lock after a tap or visibility change. It now retries automatically while visible, with 1/3/10/30-second backoff capped at 30 seconds, resets after a stable lock, and handles focus/pageshow and pending-release races. Closing or hiding the intake cancels retry timers. The interface reports actual protection status rather than promising the screen cannot lock. Unsupported browsers and OS power restrictions can still deny protection.

An established voice conversation now ends once when the page backgrounds or enters the page cache. On return, Resume speaking starts a fresh microphone connection with the existing saved draft. This deliberately avoids displaying Listening for a socket whose microphone may have been suspended. No audio-turn-based muting was reintroduced. Drafts remain in memory; browser reload/process termination still clears them. Real iPhone standby and microphone recovery have not been verified: browser tab discovery timed out before a page could be reached.

Call Scott now is always available in the panel footer, outside its scrolling content, including before consent, during the conversation and on the editable summary. It uses the existing published number 305-923-9884. A deliberate tap stops the AI session and opens the device's telephone handler; it does not submit the offer or claim a completed call. Visitors can resume their saved intake if the call is canceled or unanswered.

Website session context now asks the AI to offer Scott when the visitor expresses frustration, anger, distress, confusion or trouble being heard; a direct human request takes priority over intake questions. Acceptance leads to the direct-call control, and declining resumes the next unanswered question. A yes/no about human help cannot populate an offer field while the human-help question is pending. Transcript signals reveal the call card only: they never dial or submit anything.

This is a direct-call handoff, not a connected browser-to-telephone bridge. ElevenLabs documents its transfer_to_number tool as available only for telephone calls, not its chat widget: https://elevenlabs.io/docs/eleven-agents/customization/tools/system-tools/transfer-to-number. Seamless web-session transfer requires an authenticated telephony integration to bridge the browser audio to Scott's phone, plus answer/no-answer handling. That integration is not connected; no phone-agent configuration, phone number provisioning, call, SMS or email was performed. Do not label the current behavior an automatic transfer.

Validation: 12 targeted tests passed for wake-lock retries/backoff/cleanup, lifecycle recovery and human-help display signals. Targeted ESLint, TypeScript, production build, the existing editable-summary render test and HTTP guards passed. The HTTP test confirms the direct-call link appears outside the scroll area and retains all ten navigation destinations. Three live fictional ElevenLabs text conversations verified direct requests before intake, frustration, decline with draft preservation, and acceptance without changing the existing-agent answer. No lead or outbound contact was created. Actual voice/tone detection, mobile screen-awake behavior, phone dialing and answered-call behavior remain device tests.

Preview deployed from source `b0ab687`: https://scott-homes-f3a0ttajl-royalpalmsrealty1.vercel.app, deployment `dpl_Eu16iBLjDjGGZL4n89hFejxyJXPC`. Vercel reports READY; remote build completed in 15 seconds. Authenticated deployed HTTP returned 200 and verified all ten navigation labels, home-value action and the direct-call control outside the scroll area. Production was not promoted.

## September 14 — failed phone trial and silent-session recovery

Scott reports that source b0ab687 still locks the phone and then goes silent, including after he says he is frustrated. Turning off Low Power Mode did not resolve it. Do not claim that Low Power Mode explains the full failure or that phone voice is ready for production. The exact browser context (Safari versus an embedded browser) remains unconfirmed.

Removed the previous visibility handler's automatic endSession call. Backgrounding now preserves an established voice conversation; returning reveals recovery controls. A foreground socket still does not prove that iOS restored its audio. Reconnect voice is always available while connected and starts a fresh session with the saved draft. The OS can still suspend microphone/audio access or disconnect the call in the background. No automatic audio-turn muting or silent background reconnection was added.

Voice startup now awaits an actual granted screen wake lock, shares an in-flight request, and stops waiting after 2.5 seconds. If unsupported, denied or timed out, it offers the direct phone/written route and asks the visitor to use a browser with wake-lock support. This intentionally blocks an unprotected voice start instead of letting the visitor enter a known failure mode. It does not guarantee that an OS will never revoke a granted lock. Text does not require wake protection.

The interface displays microphone activity and reveals Reconnect voice/Call Scott after a received reply goes unanswered for 10 seconds or no dialogue activity occurs for 15 seconds. These are visual recovery controls only; they do not interrupt, mute, restart, invent a user turn or infer frustration. Actual incoming audio refreshes activity; a stale speaking flag cannot hide silence indefinitely. Human-request and frustration guidance is unchanged; if speech never reaches transcription, the agent cannot react to its content. The recovery controls do not depend on transcription.

Added /api/voice-health and a strict event-only schema. It accepts same-origin, rate-limited diagnostic events; rejects arbitrary fields and speech/draft content; and writes fixed event names to Vercel runtime logs. Events record screen-lock grants/denials/unsupported API, visibility, connection lifecycle, presence of input audio, received transcription/agent events, and delayed/quiet states. Only a page-lifetime random ID, coarse platform/browser family and frame flag accompany events. No speech, buyer fields, device IDs or full user agent are included. Audio activity is throttled to one event per 10 seconds. These events describe SDK signals, not proof that sound reached the visitor's speaker.

Validation: 18 targeted controller/schema tests passed, along with ESLint, TypeScript, production build, summary rendering and HTTP guards. HTTP tests confirm the log collector rejects speech content and foreign origins; the full navigation and persistent direct-call link remain present. This is a recovery and diagnostic improvement, not a reproduced/fixed iPhone standby result. Use runtime logs scoped to the new preview for the next report before guessing at another device-specific change. No phone calls, messages, new leads or agent-dashboard changes were made.

Recovery preview: https://scott-homes-4nrixhe1j-royalpalmsrealty1.vercel.app, deployment dpl_6HMjEy3kf722se9psy9qsDe5rWfM, source c04bd95. Vercel READY; remote build/TypeScript passed in 21 seconds. Authenticated deployed GET returned 200; navigation and direct-call link verified. A synthetic, non-customer diagnostic event using session 85fb9923-7486-413a-b984-ddb2433f0bca returned 204 and was retrieved in Vercel runtime logs, confirming the collector/readback path. This was not an iPhone/audio test. Production is unchanged.

## September 14 — final special requests

Scott confirmed that his failed phone trials opened the website inside ChatGPT. A trial directly in Safari is pending; the embedded browser is not a proven cause of the locking or silence. No additional device diagnosis or voice-lifecycle changes were made for this request.

Added Scott’s final intake question: “Any special requests, such as furniture you’d like included, or anything we haven’t covered?” Website session context asks it once before directing the buyer to review, even if the permit clause is already accepted. Already answered requests are not asked again. Answers use the existing conditions tool field, so this does not depend on the pending remote tool expansion. The complete field retains prior accepted clauses; “nothing else” adds no additional requests without canceling the permit clause. The editable summary and broker review label now say Special clauses and requests; the summary displays the question with the editable answer. Prepared dashboard prompt/tool documentation is synchronized, but no dashboard change was published.

Two live fictional text cases passed: furniture inclusion and no additional requests. Both asked the final question after the phone number, saved the response while retaining all open permits/tree permits/before-closing/seller-expense terms, and directed to review without repeating the question. Tool handlers saved only local fictional drafts; no request, lead, email, SMS or call was created. Sixteen intake unit tests, the actual summary component render and targeted ESLint passed. These results verify text/tool behavior and rendering, not iPhone audio or screen locking.

## September 14 — repeated property-address question

Scott reports that the agent asks for the address twice. Source review found conflicting instructions: the fixed remote greeting asks which property, while the website prompt unconditionally said to ask the address after the buyer says No to another agent. Removed that unconditional instruction. The agent must reuse the property answer from the greeting, save it together with the explicit No answer, and ask only the next missing detail. If only a city or unit is unclear, ask for that detail rather than the entire address. MLS unavailability is not a reason to ask again.

Current draft context now recalculates the next unanswered question on every update and explicitly supersedes older next-question instructions. Tool replies return the actual saved draft and current next question immediately after saving/MLS lookup, using latestDraft rather than a render-time snapshot. A spoken answer not yet saved must be saved instead of requested again; explicit corrections/cleared answers still take precedence over older conversation history.

Workspace maintenance removed the former local checkout. No matching Library source archive was found. Re-cloned the original repository and restored the exact prior deployed source from Vercel deployment dpl_CsJhi4e7BJm6DmBGxNk5LLKFGjmu (source f488284): 200 files already matched and 43 files were downloaded and verified against their Vercel SHA-1 hashes. The recovered baseline is commit 4b79b38 on codex/offer-address-recovery. This recovery did not change production or agent settings.

Validation: 17 intake unit tests and targeted ESLint passed. A live fictional text session supplied an address, received the existing-agent question, replied simply No after a blank draft refresh, and proceeded to offer price then escrow without repeating the address. The address and price were saved correctly in the local test draft. An initial assertion required 1000000 exactly, although 1000000.00 is also permitted; corrected the assertion to accept either valid representation and reran successfully. TypeScript passed after generating Next route types for the fresh checkout. No offer, lead, call or outbound message was created.

The original baseline text case did not reproduce the repetition, so the exact device interaction is not established. The remote fixed first message still asks which property; browser tab discovery timed out before the editor could be reached. This change corrects the website instructions and draft/tool synchronization, not the remote greeting or iPhone audio behavior. A fresh session on the new preview is required.
